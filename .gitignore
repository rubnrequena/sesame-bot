# Credenciales - NUNCA subir al repositorio
.env

# Binarios
sesame-bot
sesame-bot.exe

# Go
# go.sum DEBE versionarse: es el lockfile de verificación de módulos
# (necesario para builds reproducibles y para el build de Docker)
